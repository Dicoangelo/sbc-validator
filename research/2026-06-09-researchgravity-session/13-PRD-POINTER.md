# Pointer: the Master PRD lives on the Desktop

The extensive PRD + technical spec that fuses product + novelty + market is organized at:

**`~/Desktop/SBC-AutoOps PRD/`**
- `README.md` — the meta-prompt this package answers + how to read it
- `00-MASTER-PRD.md` — the PRD (vision, problem, novelty, ICP, full product spec, roadmap, metrics)
- `01-IMPROVEMENT-BACKLOG.md` — **for the build session:** prioritized "what we noticed," each with
  acceptance criteria + the named buyer it unlocks (B1 multi-tenant mode, B2 AMR-WB, B3 IPv6 mixed-mode,
  B4 CI/CD+GitOps, B5 HEP ingest, B6 anynode parser, B7 netFound fine-tune, B8 compliance report, ...)
- `02-MARKET-EVIDENCE-AND-LEADS.md` — warm leads, regulated users, channel/OCA, vendor duopoly (links)
- `03-LINKS-MASTER.md` — consolidated link library
- `04-ARCHIVE-AND-REVIEW-SYNTHESIS.md` — what the SBC ARCHIVE (Master Dossier) + Philip Review
  (MEETING-QA objection layer) add: the OBD-II pitch device, objection handling, advisory/BD targets
  (Schulzrinne, Pindrop), the AI-assisted-remediation + STIR/SHAKEN roadmap, and the honesty
  reconciliation (the archive oversells "AI-native/continuous/auto-remediation"; repo is the truth).

Sources: `~/Desktop/SBC Market Research/` (7 docx, ~45k words) · `~/Desktop/SBC ARCHIVE
(snapshots, superseded by repo)/` (Master Dossier + V4-V6 = earlier validator builds, NOT a separate
product) · `~/Desktop/SBC Philip Review/` (rendered review packet; MEETING-QA = the vet objection set).

This PRD is built from this research session's docs 00-12 + the market research + a live scan of the
product repo at HEAD (5 vendors, 145 tests). Kept on Desktop (not committed to the product repo) so it
stays an independent strategy artifact and does not collide with the parallel build session's tree.
